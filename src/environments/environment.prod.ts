export const environment = {
  production: true,
  environment: 'production',
  apiUrl: 'https://api.primeaawas.com',
  apiVersion: 'v1',
  appName: 'Prime Aawas',
  appVersion: '1.0.0',
  enableLogging: false,
  enableAnalytics: true,
  enableDebugMode: false,
  timeout: 30000,
  retryAttempts: 3,
  features: {
    enableNotifications: true,
    enableDarkMode: true,
    enableOfflineMode: true
  },
 
};
